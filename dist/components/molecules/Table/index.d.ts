/// <reference types="react" />
import { TableProps } from './index.d';
export declare function Table({ columns, dataSource, hidePagination, loading, onChange, onRowClick, pagination, showImportantIcon, ...rest }: TableProps): JSX.Element;
